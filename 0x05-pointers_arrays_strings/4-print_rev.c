#include "main.h"
/**
 * print_rev - prints a string in reverse
 * @s: the reverse string
 * Return: ---
 */
void print_rev(char *s)
{
	int i;

	for (i = str[-1]; i >= *str; --i)
	{
		_putchar (str[i]);
	}
	_putchar ('\n')
}
