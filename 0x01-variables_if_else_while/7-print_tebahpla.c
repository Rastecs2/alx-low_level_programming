#include<stdio.h>
#include<ctype.h>
/**
 * main - Entry point
 *
 * Return: program to print alphabets in reverse
 */

int main(void)
{
	char n = 'a';
	char l;

	while (n <= 'z')
	{
		putchar(l);
		n--;
	}
	putchar('\n');
	return (0);
}
