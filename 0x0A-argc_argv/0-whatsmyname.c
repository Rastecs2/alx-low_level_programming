#include <stdio.h>
/**
 * main - prints its name, followed by a new line.
 * @argc : arguments/ inputs to the function
 * @argv : array
 * Return : 0 (Success)
 */
int main
{
	(void)argc;
	printf("%s\n", argv[0]);
	return(0);
}
